import { Card, CardBody, CardHeader } from "@nextui-org/react"
import React from "react"

function StockDetails({ className }) {
  return (
    <Card className={`${className}`}>
      <CardHeader>
        <p className="font-bold">Details</p>
      </CardHeader>
      <CardBody>
        
      </CardBody>
    </Card>
  )
}

export default StockDetails
